services:

  app:
    build: .
    container_name: devops-compose-app
    expose:
      - "8080"

  nginx:
    image: nginx:latest
    container_name: devops-compose-nginx
    ports:
      - "8094:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
